#include<stdio.h>
#include<math.h>
int main(){
    
// interchanging w third
      // int a,c1,d1;
      // scanf("%d%d",&c1,&d1);
      // printf("c is %d and d is %d",c1,d1);
      // a=c1;
      // c1=d1;
      // d1=a;
      //printf(" \n now c is %d  and  d is %d",c1,d1);

// interchanging wo third
      // int c,d;
      // scanf("%d%d",&c,&d);
      // printf("c is %d and d is %d",c,d);
      // c=c+d;
      // d=c-d;
      // c=c-d;
      // printf(" \n now c is %d  and  d is %d",c,d);
      // // area of circle
      // float r;
      // scanf("%f",&r);
      // printf("area is %f", 3.14*pow(r,2));

//  si
//       float pa,roi,time;
//       scanf("%f%f%f",&pa,&roi,&time);
//       printf("simple interest is %f and amount is %f",pa*roi*time/100,pa+(pa*roi*time/100));

    //   ci
      // float prnc,rt,yr,trm;
      // scanf("%f%f%f%f",&prnc,&rt,&yr,&trm);
      // printf("ci is %f & final amnt is %f",pow(prnc*(1+rt/100),trm)-prnc,pow(prnc*(1+rt/trm),trm*yr));

//reversal
      // int n,m,o;
      // scanf("%d",&n);
      // o=m=0;
      // while (n!=0);
      // {
      //     o=n%10;
      //     if (o==0)
      //     printf("%d",0);
      //     m=o+m*10;
      //     n=n/10;
      // }
      // printf("%d",m);
}