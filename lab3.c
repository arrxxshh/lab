#include<stdio.h>
#include<math.h>
int main()
{
     // // sin cos log pow sqrt
     // int a;
     // scanf("%d",&a);
     // printf("%d",sin(a));
     // printf("%d",cos(a));
     // printf("%d",sin(a));
     // printf("%d",pow(a,a));
     // printf("%d",sqrt(a));    
     // // quadr
     // int a,b,c;
     // scanf("%d%d%d",&a,&b,&c);
     // int D=pow(b,2)-4*a*c;
     // printf("roots of %dx^2+%dx+%d are %f%f" ,a,b,c,(float)(-b-sqrt(D))/2*a,(float)(-b+sqrt(D))/2*a );
     //  //escape sequence
     // printf("\' \\ \t \b ");
     // // assignment operator
     // int a=2;
     // int b=5;
     // b=a;
     // printf("%d",b);
     // b+=a;
     // printf("%d",b);
     // b-=a;
     // printf("%d",b);
     // b/=a;
     // printf("%d",b);
     // b*=a;
     // printf("%d",b);

     // left shift rightshift operators
     int a=2;
     printf("%d",a>>1);
     printf("%d",a<<2);

     // //ternary operators
     // int number;
     // printf("enter a number ");
     // scanf("%d",&number);
     // (number & 1) 
     // ? printf("odd")
     // : printf("even");


}
//


